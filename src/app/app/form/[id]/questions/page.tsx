"use client";

import React from "react";

const Questions = async ({ params }: { params: { id: string } }) => {
    return (
        <div>
            <h1>Questions </h1>
        </div>
    );
};
export default Questions;
